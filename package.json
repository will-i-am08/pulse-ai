{
  "name": "pulse-agent",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.33.4",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "dev:web": "pnpm --filter @pulse/web dev",
    "dev:worker": "pnpm --filter @pulse/worker dev",
    "seed": "tsx --env-file=.env scripts/seed.ts"
  },
  "devDependencies": {
    "@pulse/shared": "workspace:*",
    "@types/node": "22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
